const gameBoard = (() => {
    const gameBoardArray = [
        ['x', 'x', 'o'],
        ['o', 'x', 'o'],
        ['o', 'o', 'x']
    ];
    return {gameBoardArray};
})();

const playerFactory = () => {
    // attributes
    // methods
    return {};
};

const displayController = (() => {
    //
})();